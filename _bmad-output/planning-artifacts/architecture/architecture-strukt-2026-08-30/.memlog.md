---
scope: strukt v1 (MVP)
purpose: build substrate for implementing strukt from PRD+UX
altitude: initiative (feeds epics/stories)
updated: 2026-08-30T15:47
---

- (direction by user) Fast path: user chose speed over coaching; spine is the only deliverable, audience is Mostafa + AI coding agents implementing stories, no separate human-facing doc needed
- (decision) Paradigm: Layered architecture with an isolated pure-computation domain core. Layers: engine/ (domain, zero framework deps) -> store/ (zustand, active-session client state only) -> lib/db/ (Drizzle+Neon persistence) + auth (Clerk) -> components/+app/ (React/Next UI). Matches the separation the existing codebase already has (engine/, store/, components/, app/)
- (decision) Auth: Clerk (native Vercel Marketplace integration, auto-provisioned env vars, prebuilt email/password + social login UI, current SDK @clerk/nextjs v7/Core 3). Chosen over Descope/Auth0 per vercel:auth skill's decision matrix -- fastest setup + prebuilt UI fits a solo-dev MVP
- (decision) Database: Neon Postgres (Vercel Marketplace native) + Drizzle ORM, node-postgres driver via Vercel Fluid Compute per neon-postgres skill's own Vercel recommendation. Pooled connection for app queries, direct/unpooled for Drizzle Kit migrations
- (decision by user) Persistence shape: Project stored as a single JSONB blob (nodes/elements/supports/loads/materials/cross-sections/unit system) keyed by id+user_id, not normalized into per-entity tables -- classroom scale (~50 elements), no cross-project entity queries needed, avoids schema churn while the engine/types.ts shape is still evolving. This ALSO resolves the UX-flagged open question (reopened Project's Cross-Section values snapshot vs live-reference): a JSONB blob captured at save time is inherently a snapshot, never a live catalog reference
- (decision by user) Canvas rendering: react-three-fiber + drei with an orthographic camera for the 2D canvas, not SVG/Canvas2D -- three/@react-three/fiber/@react-three/drei are already installed specifically because the PRD plans a post-MVP 3D transition without a rendering-engine rewrite. [ASSUMPTION: inferred from existing deps + PRD's own 'three.js already a dependency, groundwork exists' reasoning, not explicitly stated as the plan -- flag if SVG/Canvas2D was actually intended for crisper DESIGN.md hairline/dash-pattern work]
- (decision by user) Equation rendering for Show Steps: KaTeX -- faster, smaller bundle, synchronous rendering (no page reflow), strong matrix/DOF-notation support, verified current via web search 2026-08-30. MathJax's MathML/screen-reader accessibility edge noted but not decisive since the Show-Steps screen-reader gap is already an accepted-open UX item, not something this pick needs to close
- (decision) [ADOPTED] Naming reconciliation: engine/types.ts becomes the single source of truth for all shared enums/types (Support, StructureType, MaterialKind); engine/constants.ts keeps only numeric config values keyed by those types, no redeclared types. Support enum key becomes HINGE (matching constants.ts and the UX-settled 'Hinged' UI label), not PINNED -- resolves the PRD/UX-flagged three-way naming inconsistency (Support, StructureType, Material/MaterialType) in one pass
- (decision) Units: engine/solver always operates in SI internally regardless of the project's display unit system; Imperial is a presentation-layer-only conversion at the UI boundary, never reaches the solver. Prevents round-trip drift and inconsistent-unit solver bugs (PRD FR-20)
- (decision) Solver contract: a single solve() call returns final results AND every intermediate quantity Show Steps needs (per-element local stiffness matrices, DOF mapping, global assembly, boundary-condition-reduced system) in one SolveResult shape -- UI never recomputes or re-derives Show Steps content independently, preventing exactly the drift-from-solver risk the PRD flags as a trust-breaking bug
- (decision) Cross-Section catalog is static bundled reference data (a versioned data module, not a database table) -- fixed content, never user-edited, no DB round-trip needed
- (decision by user) Project CRUD (save/load/list/delete) via Next.js Server Actions calling Drizzle directly -- no client data-fetching library (React Query/SWR) introduced; zustand store holds only the active canvas's editable session state, never the Projects list, avoiding two sources of truth for what Projects exist
- (decision) Benchmark/regression validation suite (PRD FR-11 NFR, SM-4) lives as an automated Vitest suite against engine/, run in CI -- not a UI-facing feature
- (decision by user) Load entity model (resolves the addendum's explicit mz question and FR-8/FR-9's multiple-loads-sum requirement): Load becomes a first-class entity {id, kind: concentrated|udl, target: {type:node,nodeId}|{type:element,elementId}, magnitude, direction}, replacing StructuralNode.fx/fy/mz scalar fields entirely. mz is REMOVED (addendum option c) -- moment loads are an explicit FR-9 non-goal and reaction moments already have their own NodeResult.rmz slot, so an input-side mz was leftover/premature, not reserved for anything real
- (decision by user) Global sign/direction convention (PRD delegated this to architecture): standard math/engineering convention -- positive x = rightward, positive y = upward, in the structure's global coordinate system. Same convention crosses into Show Steps display (FR-17-19) and the FR-15 equilibrium check
- (decision by user) Solver invocation site (PRD explicitly asked architecture to confirm): FR-10's interactive Solve runs client-side, synchronous, directly against engine/ -- confirms the PRD's cost-assumption (backend cost stays limited to auth+storage). No Server Action re-runs solve() for the interactive path; AD-1's 'safe to run in a Server Action too' is a portability property only, not a v1 code path
- (decision by user) SolveResult shape pinned precisely: keyed by entity id (not array index) for every per-Node/per-Element field; plain number[]/number[][] only, no mathjs Matrix crossing the solve() return boundary (keeps it JSON-serializable, prevents a second silent recomputation of free-DOFs); explicit freeDofs: string[] field so no consumer re-derives it from Supports independently. Solve results are session-only, never persisted in a Project's saved JSONB -- reopening a Project always re-solves before Show Steps works
- (decision by user) Project JSONB document gets a mandatory schemaVersion:number field from day one (cheap insurance against the exact schema-churn risk AD-5 already names). Cross-Section resolution: denormalized inline per-Element (full resolved catalog entry copied onto each Element) chosen over a deduplicated root-level dictionary -- simpler to reason about at ~50-element MVP scale, dedup is a viable future optimization not a v1 requirement
- (decision by user) Concurrent Save: last-write-wins is explicitly accepted for MVP (not silently -- stated as a deliberate choice), rather than adding optimistic-concurrency checking. Low real risk for a solo-student single-project-at-a-time tool; revisit if multi-tab/collaborative editing becomes a real usage pattern
- (decision by user) user_id column type: text, storing Clerk's userId string verbatim -- explicitly exempt from the spine's own Ids-are-UUID convention since it's a foreign identity, not a system-generated entity id (Clerk ids aren't UUID-formatted)
- (decision by user) Server Actions boundary tightened: ALL FOUR Project operations (List included, not just Save/Delete) must be named exported functions in app/**/actions.ts -- no inline Drizzle query in a Server Component or elsewhere, closing the RSC-direct-read loophole
- (event) Promoted AD-9 (benchmark suite, already decided earlier in this run) from memlog-only into an actual spine section -- it was cited 3x in the draft but never written
- (decision) Stack table corrections: Neon driver pinned to pg (node-postgres) via @vercel/functions attachDatabasePool, not hedged as 'or @neondatabase/serverless' -- matches Vercel's current Fluid Compute guidance. Verified-current versions recorded explicitly instead of 'latest stable': KaTeX 0.16.22, Drizzle ORM 0.45.2, Vitest 4.1 (all independently confirmed 2026-08-30, closing the unearned-confidence flag on Drizzle/Vitest specifically)
- (event) Reviewer Gate resolved: applied all fixes from lint_spine.py (frontmatter placeholder paths), input reconciliation (8 gaps), rubric review (1 critical + 3 high + 4 medium + 2 low), tech-verification (Drizzle/Vitest/KaTeX versions pinned, Neon driver hedge collapsed), and adversarial review (7 incompatible-pair holes closed: SolveResult shape pinned, JSONB schema versioned, user_id typed text not UUID, Server Actions cover List, units read/write-through only, concurrent-save policy stated, solve() invocation site confirmed client-side). Added AD-9 (benchmark suite, promoted from phantom reference) and AD-10 (Load entity + mz removal + sign convention, the one critical gap). Fixed AD-2's Material/MaterialKind self-contradiction, the Deferred section's anonymous-canvas contradiction with AD-6, and added Safety-disclaimer/dark-mode acknowledgment. spine finalized
- (event) spine finalized
